# MVU（MagVarUpdate）框架技术原理

本文档从工程角度说明 SillyTavern 角色卡里 **MVU（MagVarUpdate）** 变量驱动框架的
运作原理，依据 `tavern_cards.db` 中已导入的卡片结构整理。MVU 本身是**主题无关的
通用变量管理框架**，本文只讨论它的技术机制。

## 总览

MVU 把一次对话变成一个**有状态的模拟**：维护一份结构化「变量树」，每回合由 AI
按规则产出「变量更新指令」，引擎校验并应用这些指令，再把最新状态渲染成 UI。整体
是一个 **prompt 注入 → AI 产出补丁 → 引擎应用 → 渲染** 的闭环。

```
            ┌─────────────────────── 注入到 prompt ───────────────────────┐
            │  变量列表(YAML schema) + 更新规则 + 输出格式 + 当前变量快照     │
            ▼                                                              │
   ┌──────────────┐   <UpdateVariable> 内的 JSON Patch    ┌──────────────┐ │
   │      AI       │ ───────────────────────────────────▶ │  MVU 引擎     │ │
   │  (生成正文 +  │                                       │  bundle.js    │ │
   │   更新指令)   │                                       └──────┬───────┘ │
   └──────────────┘                                              │         │
            ▲                                          COMMAND_PARSED 事件  │
            │                                                     ▼         │
   ┌────────┴────────┐   渲染 <StatusPlaceHolderImpl/>   ┌──────────────┐  │
   │   状态栏 / UI    │ ◀──────────────────────────────  │ 业务脚本 +    │  │
   │  (regex 注入)    │                                   │ 核心数值保护  │  │
   └─────────────────┘                                   └──────┬───────┘  │
                                                  应用补丁、更新变量树 ─────┘
```

## 分层结构

### 1. 引擎层（Engine）

由 `MUV` 这个 TavernHelper 脚本一行引入：

```js
import 'https://.../MagVarUpdate/artifact/bundle.js';
```

引擎负责：维护变量树、解析 AI 输出中的更新指令、触发事件、按「楼层」（每条消息）
持久化变量快照。`MUV` 脚本还声明了一组操作按钮（`重新处理变量`、`快照楼层`、
`重演楼层`、`清除旧楼层变量` 等），对应引擎的状态管理能力——这使得回退/重演某一
楼层时变量能正确还原。

### 2. Schema 注册层（数据模型）

`变量结构` 脚本用 **zod** 定义强类型数据模型并注册给引擎：

```js
import { registerMvuSchema } from '.../mvu_zod.js';
const r = z.enum(['抵抗','动摇','沉溺','疯狂','圆满']);   // 枚举
const s = z.object({                                      // 嵌套对象 + 默认值
  上装: z.string().default('...'),
  暴露程度: z.enum(['保守','正常','清凉','暴露','极度暴露']).default('正常'),
  ...
}).describe('...');                                        // 给 AI 的字段说明
registerMvuSchema(z.object({ 世界: ..., /* 角色状态 */ ... }));
```

作用：
- **类型校验与强制转换**（`z.coerce.number()` 等），保证 AI 写入的值合法；
- **默认值**：新对象/新字段自动填默认；
- **`.describe()` 注解**：作为字段语义说明，最终汇入给 AI 的提示。

### 3. 初始化层（initvar）

`[initvar]` 世界书条目以 YAML 给出**初始变量值**（如 `当前阶段: 1`、起始服装、
各项数值起点）。新对话开始时，引擎用它给变量树播种。注意该条目本身在世界书里
是 `enabled=false`——它不是给 AI 读的常驻内容，而是被引擎专门识别的初值来源。

### 4. 更新管线（核心）

**(a) 约定输出格式** —— `[mvu_update]变量输出格式` 世界书条目要求 AI 在每次回复
结尾输出一个固定块，指令格式是 **JSON Patch (RFC 6902)** 的变体：

```
<UpdateVariable>
<Analysis> ... (英文，<120 词，解释本回合各变量为何变化) </Analysis>
<JSONPatch>
[
  { "op": "replace", "path": "/世界/时间",   "value": "08:00" },
  { "op": "delta",   "path": "/.../潜意识侵蚀度", "value": 5 },
  { "op": "insert",  "path": "/.../习惯列表", "value": {...} },
  { "op": "remove",  "path": "/.../临时标记/0" }
]
</JSONPatch>
</UpdateVariable>
```

支持的操作：`replace`（替换）、`delta`（数值增量）、`insert`（插入）、`remove`
（删除）。以 `_` 开头的字段为只读。

**(b) 引擎解析并触发事件** —— 引擎解析出指令数组后触发 `COMMAND_PARSED` 等事件。

**(c) 守卫拦截** —— `核心数值保护` 脚本挂在该事件上做「服务端校验」：

```js
eventOn(Mvu.events.COMMAND_PARSED, (msgId, commands, ...) => {
  // 倒序遍历，凡是指向受保护字段的指令一律 splice 删除
  // 受保护字段：世界.时间/日期/星期、各角色核心数值(道德底线/各依存度/侵蚀度/
  //            阶段/标题)、念头培育区里的计时字段(开发进度/需要时间/过期时间)
});
```

这保证了**脚本独占控制的字段**（时间推进、阶段计算、计时器）不会被 AI 擅自篡改——
AI 只能影响「软」状态，硬数值由代码按规则推进。

**(d) 应用补丁** —— 通过校验的指令最终改写变量树并持久化到当前楼层。

### 5. 规则注入层

`变量列表`（YAML 字段清单，含类型与取值范围）与 `[mvu_update]变量更新规则`
（逐字段的变化规则，如「侵蚀度每 20 点对应 1 个阶段」「时间禁止 AI 修改、由脚本
推进」）作为常驻世界书内容注入提示，让 AI 知道**模型形状**和**变化规则**。这与
第 4(a) 步的输出格式一起构成「AI 该写什么、怎么写」的契约。

### 6. 渲染层（regex）

引擎只管数据，UI 由 **regex 脚本**（`placement:[2]`，作用于 AI 输出）完成：

- **`状态栏`**：把占位符 `<StatusPlaceHolderImpl/>` 替换成一段内嵌 HTML/JS 的
  组件（`replaceString` 长达 ~87KB）。该组件运行时读取当前变量树，渲染成可视化
  状态面板。
- **`去除变量更新`**：用正则
  `/<UpdateVariable>[\s\S]*?<\/UpdateVariable>|<status_current_variables>[\s\S]*?<\/status_current_variables>/`
  把原始更新指令块从**显示文本**中删掉——引擎照常处理这些指令，但用户看到的是
  干净正文。
- **`对AI隐藏` 系列**：用 `promptOnly`/`markdownOnly` 等开关控制某些内容只在
  显示层或只在送给 AI 的提示层出现，实现「信息隔离」。

### 7. 业务逻辑层

`游戏逻辑`（~45KB）、`习惯注入脚本`（~37KB）是挂在引擎事件上的自定义脚本，负责
纯代码计算：时间/日期/星期推进、阶段阈值判定、NPC 作息与巡逻调度、派生状态注入
等。配合「念头/习惯」等世界书系统条目（多为 `constant=true` 常驻），形成完整玩法。

## 一句话总结

MVU 的本质是：**用 zod 定义一份带类型/默认值的状态 schema → 用世界书把 schema、
规则、输出格式注入提示 → 让 AI 以 JSON Patch 增量更新状态 → 用事件钩子做服务端
校验保护硬数值 → 用 regex 把状态渲染成 UI 并隐藏原始指令**。数据由引擎与脚本权威
控制，AI 只在规则约束内驱动「软」叙事变量。

## 关键文件/字段对照（本卡）

| 角色 | 组件 | 作用 |
| --- | --- | --- |
| 引擎 | helper `MUV` | 引入 MagVarUpdate bundle |
| 数据模型 | helper `变量结构` | zod schema 注册 |
| 守卫 | helper `核心数值保护` | `COMMAND_PARSED` 拦截受保护字段 |
| 业务 | helper `游戏逻辑` / `习惯注入脚本` | 时间推进、阶段、作息等计算 |
| 初值 | WB `[initvar]` | 变量树初始播种 |
| 契约 | WB `变量列表` / `变量更新规则` / `变量输出格式` | schema、规则、JSON Patch 格式 |
| UI | regex `状态栏` | 渲染 `<StatusPlaceHolderImpl/>` |
| 清理 | regex `去除变量更新` | 从显示层剥离更新指令 |
